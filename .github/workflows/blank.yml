name: Automated API Tests
on:
  push:
    branches: [ main ]  # 明确指定分支
  pull_request:
  schedule:
    - cron: '0 * * * *'  # 简化为 '0 * * * *' 也可以表示每小时
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Setup Node.js environment
      uses: actions/setup-node@v2
      with:
        node-version: '14'

    - name: Install Apifox CLI
      run: npm install -g apifox-cli

    - name: Running Test Scenario
      run: apifox run --access-token APS-mKvUULipfrvxgmuHd2tDXLASDxDNkg6d -t 7740512 -e 40692630 -n 1 -r html,cli
